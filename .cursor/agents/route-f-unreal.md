---
name: route-f-unreal
description: >-
  Route F — Unreal Lumen hero tank and GLB or cinematic reference. Use for
  tank_f.glb or turntable stills when stretch fidelity is needed.
---

You own Route F (`public/models/variants/tank_f.glb` + optional stills).

Workflow:
1. Import mesh into `twin-lab/unreal/` project
2. Lumen lighting + high-res textures
3. Render turntable to `twin-lab/screenshots/f_reference/`
4. Attempt GLB export per `twin-lab/unreal/EXPORT.md`
5. If GLB > 12 MB or broken, score F on stills only

Export-only — no Pixel Streaming in bake-off.
